--changeset TUTSK:1
CREATE TABLE IF NOT EXISTS test45 (
                                     id BIGINT PRIMARY KEY,
                                     name VARCHAR(50) NOT NULL,
    price BIGINT NOT NULL
    );